/******************************************************************************
 *
 * IG - Informática Gráfica
 * Grado en Ingeniería Informática
 *
 * 2014 - Ernesto Serrano <erseco@correo.ugr.es>
 * ---------------------------------------------
 * 
 *  Fichero de muestra del esqueleto para las prácticas
 *  
 ******************************************************************************/
#include <cstdlib>
#include <iostream> 
#include <iomanip> 
#include <pthread.h> 
#include "fun_tiempo.h" // Funciones de calculo de tiempo
#include <math.h> // Incluimos las librerías matemáticas

using namespace std;

int main(int argc, char *argv[]) {

    // Comprobamos que se le pasen los parametros correctos al programa
    if (argc < 2) 
    {
        cout << "Uso del programa: " << argv[0] << " <cond>" << endl << endl;
        exit(2); // Salimos con error 2
    }

    // Asignamos el numero de muestras


    // Hacemos los calculos


    // Mostramos los valores obtenidos

    // Salimos del programa
    exit(0);

}